name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: install-oj
      run: pip3 install --user online-judge-tools
    - name: run-oj
      run: cd test-oj && ./test.sh
